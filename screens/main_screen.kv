<MainScreen>
    SelectButton:
        pos_hint: {"center_x": .25, "center_y": .85}
        type: "abc"
        main_screen: root
        id: select_0

    SelectButton:
        pos_hint: {"center_x": .5, "center_y": .85}
        type: "category"
        main_screen: root
        id: select_1

    SelectButton:
        pos_hint: {"center_x": .75, "center_y": .85}
        type: "translation"
        main_screen: root
        id: select_2

    ABCOptions:
        id: abc_options
        pos_hint: {"center_x": -2}

    TranslationOptions:
        id: translation_options
        pos_hint: {"center_x": -2}

    CategoryOptions:
        id: category_options
        pos_hint: {"center_x": -2}

    MDRectangleFlatButton:
        pos_hint: {"center_x": 0.5, "center_y": 0.1}
        on_press: root.choose_mode()
        text: "Start"


<ABCOptions>:
    size_hint: 0.8, 0.4
    GridLayout:
        cols: 2
        MDLabel:
        MDTextField:
        MDLabel:
        MDTextField:

<TranslationOptions>:
    size_hint: 0.8, 0.4
    GridLayout:
        cols: 2
        MDLabel:
        MDTextField:
        MDLabel:
        MDTextField:

<CategoryOptions>:
    size_hint: 0.8, 0.4
    GridLayout:
        cols: 2
        MDLabel:
        MDTextField:
        MDLabel:
        MDTextField:
